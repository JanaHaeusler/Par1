Enabled Main Button

```jsx
import { CheckIcon } from '../Icons/Icons'
;<Button main onClick={() => {}}>
  <CheckIcon />
  Save
</Button>
```

Disabled Main Button

```jsx
import { CheckIcon } from '../Icons/Icons'
;<Button main onClick={() => {}} disabled>
  <CheckIcon />
  Save
</Button>
```

Enabled Button

```jsx
import { CheckIcon } from '../Icons/Icons'
;<Button onClick={() => {}}>
  <CheckIcon />
  Save
</Button>
```
